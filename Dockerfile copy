FROM ubuntu:latest
LABEL maintainer="Dausyaf Aryandha"

# dia akan dijalankan ketika membangun image
RUN apt-get update

# ketika container dijalankan, maka perintah ini akan dijalankan
CMD ["echo", "Hello World! dari Dockerfile! dua"]